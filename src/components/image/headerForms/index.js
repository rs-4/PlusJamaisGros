import React from 'react';
import {Button, Text, TextInput, View, Image, StyleSheet} from 'react-native';
import styled from 'styled-components';

const HeaderForms = () =>  {
  return (
    <Image source={require('../../../public/header.png')} />
  );
};

export default HeaderForms;